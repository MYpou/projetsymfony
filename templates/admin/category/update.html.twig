{% extends 'base.html.twig' %}

{% block title %}Modifier une Catégorie{% endblock %}

{% block main %}

<h2>Modifier les données d'une catégorie</h2>

{# <form method="POST">
  <div>
    <label for="name">Nom: </label>
    <input type="text" name="name" id="name" value="{{ author.name }}">
  </div>
  <div>
    <label for="description">Description: </label>
    <textarea name="description" id="description" cols="30" rows="10">{{ author.description }}</textarea>
  </div>
  <div>
    <label for="imageUrl">Image: </label>
    <input type="text" name="imageUrl" id="imageUrl" value="{{ author.imageUrl }}">
  </div>
  <button type="submit">Modifier</button>
  <button><a href="{{ path('app_author_delete', {'id': author.id}) }}">Supprimer</a></button>
</form> #}

{{ form(view) }}
<button><a href="{{ path('app_admin_category_delete', {'id': category.id}) }}">Supprimer</a></button>

<a href="{{ path('app_admin_index') }}">Retour à l'accueil</a>


{% endblock %}